import tkinter as tk

# Anything that we put in the app will be run between these two lines of code
root = tk.Tk()

button = tk.Button(root, text="Test Button", bg='crimson')  # Creating button
button.pack()  # That will put the button on the screen

root.mainloop()
# Anything that we put in the app will be run between these two lines of code
